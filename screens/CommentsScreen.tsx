import { View, Text, SafeAreaView } from 'react-native';

const CommentsScreen = () => {
  return (
    <SafeAreaView>
      <Text>Comments</Text>
    </SafeAreaView>
  );
};

export default CommentsScreen;
